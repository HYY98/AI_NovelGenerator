# 设定卡与章节编辑

本地代码基于 https://github.com/YILING0013/AI_NovelGenerator 的 main 分支源码压缩包，未连接 Git 远程仓库或推送修改。

## 启动

已安装项目依赖的环境可运行 `python main.py`。本工作区依赖安装到 `.deps` 时，运行 `powershell -ExecutionPolicy Bypass -File .\start_local.ps1`。

## 设定卡库

先在主界面设置小说保存路径，再打开“设定卡库”，点击“加载小说”。三类卡共用管理界面，通过类型菜单切换：

- 地点卡：名称、所属区域、环境与外观、用途与剧情作用、关联人物与地点、备注。
- 物品卡：名称、物品类别、外观、功能与能力、限制与代价、持有者与归属、备注。
- 规则设定卡：名称、适用范围、规则内容、约束与代价、例外条件、违反后果、备注。

支持新增、修改、删除、全文搜索、JSON 导入导出。同类型名称不可重复。导入可合并或全部替换，重复或格式错误会阻止整次导入，不会只写入部分内容。

卡片保存在小说目录下的 `setting_cards.json`，每本小说独立。勾选“生成时启用”的已保存卡片会进入第一章与后续章节的自动提示词，也参与一致性审校。停用卡仍保留在卡库。卡片库损坏时会报告错误，不会静默跳过设定。用户明确使用自定义提示词时保留其原文，不额外追加卡片。

保存路径改动后，卡片编辑仍绑定当前已加载的小说；点击“加载小说”才会切换。切换卡片、卡片类型、加载小说或关闭窗口时，存在未保存内容会询问保存、放弃或取消。

## 章节编辑

“章节编辑”页读取小说目录下的 `chapters/chapter_编号.txt`，按章节编号排序，支持上一章、下一章、新建、删除、刷新、保存、字数统计、查找、替换、全部替换、撤销及重做。保存保留原始空白与换行。

编辑区支持 Ctrl+S 保存、Ctrl+F 查找、Ctrl+Z 撤销、Ctrl+Y 重做。全部替换可一次撤销。跨章节切换会清空撤销历史，避免把其他章节文本带入当前章节。

未保存保护覆盖切换、刷新、新建与窗口关闭。保存采用同目录临时文件再原子替换；若文件已被生成流程或其他编辑器修改，阻止覆盖并保留编辑内容。请保留需要的编辑内容，再刷新加载最新文件后合并。

保存路径改动不会让已加载章节写入其他小说；刷新才会加载新路径的章节。手动编辑章节不会自动更新前文摘要、角色状态或向量库，需要通过原有定稿流程更新这些衍生内容。

## 验证

使用已安装依赖的环境执行 `python -m unittest discover -s tests -v`；工作区 `.deps` 环境先设置 `$env:PYTHONPATH = "$pwd\.deps"`。

本次验证：46 项测试全部通过，无跳过项；`python tests/smoke_desktop.py` 完整主窗口、卡片保存、章节保存、两种窗口尺寸和退出检查通过。本机 `.deps` 中 gRPC 使用 1.71.0，以避开自动解析到的 1.84.0 原生 DLL 加载失败；没有修改上游 requirements.txt。

模型生成和审校需要在配置页填写有效的服务地址、模型名称和 API 密钥。离线测试不验证模型服务的实际响应质量。
