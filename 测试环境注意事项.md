# 测试环境注意事项（AI_NovelGenerator_17777）

> 服务器：106.75.37.202（Ubuntu 24.04，Docker 29.x）
> 部署目录：`/home/ubuntu/AI_NovelGenerator_17777`（GitHub 私密仓库 HYY98/AI_NovelGenerator 的 **dev 分支** Web 版）
> 技术栈：FastAPI（后端）+ Next.js 16（Turbopack，前端）+ MongoDB（Docker）
> 最后更新：2026-09-15（补充设定卡三层、章节三层、业务ID关联口径、旧版迁移、章节与设定卡 AI 闭环）

---

## 一、多实例端口对照表（最重要，别连错）

本机同时跑了两套 AI_NovelGenerator（14444 旧实例 / 17777 dev 实例），**所有端口必须成对隔离，串一个就会连到另一套库**。

| 用途 | 14444 实例 | 17777 实例（本目录） |
| --- | --- | --- |
| 部署目录 | `/home/ubuntu/AI_NovelGenerator` | `/home/ubuntu/AI_NovelGenerator_17777` |
| 对外前端端口 | 0.0.0.0:**14444** | 0.0.0.0:**17777** |
| 后端 systemd 服务 | `ai-novel-backend` | `ai-novel-backend-17777` |
| 前端 systemd 服务 | `ai-novel-frontend` | `ai-novel-frontend-17777` |
| 后端监听（仅本机） | 127.0.0.1:**8200** | 127.0.0.1:**8300** |
| MongoDB 容器 | `ai-novel-mongo` | `ai-novel-mongo-17777` |
| MongoDB 端口（仅本机） | 127.0.0.1:**27017** | 127.0.0.1:**27018** |
| 数据库名 | my_database | my_database |

> 内部服务（后端、MongoDB）一律只绑 `127.0.0.1`，不对外暴露；只有前端 Next.js 绑 `0.0.0.0` 对外。

---

## 二、数据库与数据存储

### 1. MongoDB 用 Docker 跑，数据在容器内

- 17777 实例容器：`ai-novel-mongo-17777`，宿主机映射 `127.0.0.1:27018 -> 容器 27017`。
- 进入 mongo shell：
  ```bash
  docker exec -it ai-novel-mongo-17777 mongosh
  use my_database
  show collections
  ```
- 业务集合：`novels`、`characters`、`factions`、`volumes`、`setting_cards`（地点/物品/规则卡）、`chapters`（章节）、`id_sequences`（业务ID自增序列）。
- **所有业务文档统一软删除**：查询默认带 `is_deleted: false`，删除是把 `is_deleted` 置 true 并写 `deleted_at`，不是物理删除。排查"列表为空但创建报重名"时，记得把软删除文档也算进去看（查询带 `include_deleted=true`）。

### 2. 连接串（config.yaml）

```yaml
mongodb_url: mongodb://127.0.0.1:27018     # 注意是 27018，不是 27017
mongo_database_name: my_database
mongo_timeout_ms: 5000
mongo_transaction_mode: auto
```

### 3. 业务ID与 id_sequences（重点）

所有面向用户的实体**不用 Mongo ObjectId 对外**，而是用可读的业务ID，由 `id_sequences` 集合按 `novel_id + entity_type` 维度自增分配（6 位补零）：

| 实体 | 业务ID字段 | 前缀 | 示例 | id_sequences 的 entity_type |
| --- | --- | --- | --- | --- |
| 角色 | `character_id` | `char_` | char_000001 | character |
| 势力 | `faction_id` | `fac_` | — | faction |
| 地点卡 | `card_id` | `loc_` | loc_000001 | setting_card_location |
| 物品卡 | `card_id` | `itm_` | itm_000001 | setting_card_item |
| 规则卡 | `card_id` | `rul_` | rul_000001 | setting_card_rule |
| 章节 | `chapter_id` | `chap_` | chap_000001 | chapter |

- `id_sequences` 文档结构：`{novel_id:ObjectId, entity_type, value:整数, created_at, updated_at}`，序列按小说隔离，换一本小说从 000001 重新计。
- 文档内部的 `novel_id`、`volume_id` 等**关联引用仍存 ObjectId**；业务ID只用于"对外展示 + 章节关联实体"（见第六节坑）。

---

## 三、踩过的坑（务必先看）

### 坑 1：MongoDB 客户端是「异步」的，Repository 必须 async/await

后端用的是 **PyMongo Async**（`AsyncMongoClient` / `AsyncDatabase`），不是 motor，也不是同步 pymongo。

- collection 上的 `find_one / find / insert_one / count_documents` 全是协程，**必须 `await`**。
- 新增 Repository 直接继承 `backend/db/base.py` 的 `BaseRepository`（已封装 `find_one/find_many/insert_one/update_one/soft_delete_one/count_documents`，默认排除软删除），`__init__` 只记集合名，用 `@property collection` 延迟取库。

### 坑 2：不要在 Repository 的 __init__ 里连数据库

`main.py` import 阶段就实例化 repository，此时还没 `connect_to_mongo()`，在 `__init__` 调 `get_database()` 会让后端起不来：`RuntimeError: MongoDB client is not initialized`。

### 坑 3：localhost 和 127.0.0.1 在多实例下不是一回事

14444 用 `mongodb://localhost:27017`，17777 用 `mongodb://127.0.0.1:27018`，复制配置时端口必须跟着实例改，否则数据串台。

### 坑 4：前端 next.config.ts 的 rewrite 后端端口要随实例改

```ts
async rewrites() {
  return [{ source: "/api/:path*", destination: "http://127.0.0.1:8300/api/:path*" }];
}
```
**改任何前端代码都必须重新 `npm run build` 再重启前端服务**，Next.js 生产模式跑的是构建产物，不 build 不生效。

### 坑 5：config.yaml 含密钥，绝不能进 Git

- `backend/config/config.yaml` 里有 TeamoRouter 的 `sk-teamo-*`、火山方舟 `ark-*` 等真实密钥，已在 `.gitignore` 忽略，**不要 `git add -f`**。
- 对外只提供脱敏模板 `backend/config/config.example.yaml`。
- 提交前自查，确认待提交文件里不含任何真实密钥、服务器/账号密码（例如 `sk-` 开头的 Key、`ark-` 开头的方舟 Key、登录口令等），命中 0 条才算干净：
  ```bash
  # 按本机实际使用的密钥/口令前缀自行补充模式，下面仅示例结构
  git grep -n -E 'sk-[A-Za-z0-9]{16,}|ark-[0-9a-f]{8}' || echo "源码无硬编码密钥"
  git diff --cached | grep -iE 'password|passwd|secret|token'   # 人工确认这些位置不是真实凭据
  ```

### 坑 6：跑运维脚本要用后端 venv，且 venv 里没有 motor

- 系统 `python3` 没装 pymongo，**一次性运维/查数脚本必须用后端虚拟环境**：`/home/ubuntu/AI_NovelGenerator_17777/.venv/bin/python xxx.py`。
- 该 venv 装的是**同步 pymongo**（没有 motor、也不依赖异步事件循环），所以临时查数脚本用同步写法最省事：
  ```python
  from pymongo import MongoClient
  cli = MongoClient("mongodb://127.0.0.1:27018"); db = cli["my_database"]
  ```
- 只有"复用后端 Repository（异步）"的脚本（如迁移脚本）才需要 `asyncio.run` + `connect_to_mongo()`。

### 坑 7：章节关联实体必须用「业务ID」，不能用 ObjectId（前端踩过 null 的坑）

- 章节的 `linked_character_ids / linked_location_ids / linked_item_ids / linked_rule_ids` 统一存**业务ID**：角色存 `char_xxx`，三类卡存 `loc_/itm_/rul_xxx`。
- **角色接口 `GET /api/characters/novel/{id}` 返回 `{"data":[...]}`，且角色对象没有 `_id` 字段，唯一标识是 `character_id`**；设定卡接口直接返回数组，对象同时有 `_id`（ObjectId）和 `card_id`（业务ID）。
- 前端曾统一取 `_id`，导致角色关联发出去是 `{"linked_character_ids":[null]}`（存不进）。正确做法：角色取 `character_id`、卡取 `card_id`。
- 判断"是否选中"也要用同一套业务ID，否则刷新后高亮对不上。

### 坑 8：React 受控组件用脚本赋值要触发原生 setter

浏览器自动化里给 input/textarea 赋值，直接 `.value=x` 不会触发 React 的 onChange，必须用原生 value 描述符再派发 input 事件；select 派发 change 事件。`window.confirm` 先 `window.confirm=()=>true` 覆盖，否则删除流程会被原生弹窗卡住。

---

## 四、常用运维命令

```bash
# 重启后端 / 前端
sudo systemctl restart ai-novel-backend-17777
sudo systemctl restart ai-novel-frontend-17777

# 看状态 / 实时日志
sudo systemctl is-active ai-novel-backend-17777 ai-novel-frontend-17777
sudo journalctl -u ai-novel-backend-17777 -f

# 后端改了代码：重启后端即可（FastAPI）
# 前端改了代码：必须重新构建再重启
cd /home/ubuntu/AI_NovelGenerator_17777/frontend
npm run build
sudo systemctl restart ai-novel-frontend-17777

# 本机自测后端接口
curl -s http://127.0.0.1:8300/api/setting-cards/types | python3 -m json.tool
```

---

## 五、更新代码 / 发布流程（服务器版本要保持最新）

```bash
cd /home/ubuntu/AI_NovelGenerator_17777
git fetch origin
git checkout dev
git pull origin dev
# 后端有改动 → 重启后端；前端有改动 → cd frontend && npm run build，再重启前端
```

> `config.yaml` 被 gitignore，`git pull` 不会覆盖本机真实配置。
> 若在服务器上直接改了代码：`git add` 对应源码（**不要 add config.yaml**）→ 脱敏检查 → commit → `git push origin dev`，保证 GitHub 与服务器一致。

---

## 六、设定卡（setting_cards）

地点卡 / 物品卡 / 设定规则卡共用一个集合，靠 `type` 区分。权威字段定义在后端 `backend/db/repositories/setting_card_repository.py` 的 `CARD_TYPES`，前端 `StoryContinuityWorkspace.tsx` 的字段元数据必须与它一致。

### 1. 文档结构

```json
{
  "card_id": "itm_000001",
  "novel_id": "ObjectId",
  "type": "item",
  "name": "噬渊核心",
  "aliases": ["核心", "虚无之心"],
  "fields": { "category": "", "appearance": "", "...": "各类型扩展字段" },
  "enabled": true,
  "importance": 5,
  "first_appearance_chapter": "",
  "current_state": "完好",
  "state_history": [ { "from": "", "to": "完好", "note": "", "chapter": "" } ],
  "tags": [],
  "sort_order": 0,
  "version": 1,
  "is_deleted": false,
  "created_at": "...", "updated_at": "...", "deleted_at": null
}
```

### 2. 三类卡扩展字段（fields 内的 key）

- **地点卡 location**（状态候选：开放/封锁/已毁/迁移/未知，13 项）：
  `location_type, region, parent_location, appearance, atmosphere, geographic_features, purpose, access_conditions, danger_factors, related_characters, related_factions, related_items, notes`
- **物品卡 item**（状态候选：完好/受损/已损毁/遗失/封印中，13 项）：
  `category, appearance, origin, maker, owner, abilities, limitations, cost, cooldown, quantity, durability, secret, notes`
- **规则卡 rule**（状态候选：生效中/已失效/待揭示，10 项）：
  `rule_category, scope, definition, trigger, constraints, exceptions, consequences, priority, is_hard_rule, notes`

### 3. API（前缀 /api/setting-cards）

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/types` | 三类卡字段蓝本 |
| GET | `/stats/{novel_id}` | 分类计数 |
| GET | `/{novel_id}?type=&query=&include_deleted=&enabled_only=` | 列表（默认不含已删） |
| POST | `/{novel_id}` | 新建（自动分配 card_id，同 novel+type 下 name 唯一，重复 409） |
| GET | `/{novel_id}/{cid}?include_deleted=` | 详情 |
| PUT | `/{novel_id}/{cid}?expected_version=N` | 更新（乐观锁，版本不符 409；current_state 变化自动追加 state_history） |
| DELETE | `/{novel_id}/{cid}` | 软删 |
| POST | `/{novel_id}/{cid}/restore` | 恢复 |
| DELETE | `/{novel_id}/{cid}/hard` | 物理删除（仅回收站内） |

---

## 七、章节（chapters）

### 1. 文档结构

```json
{
  "chapter_id": "chap_000001",
  "novel_id": "ObjectId",
  "volume_id": null,
  "number": 1,                         // 同小说内唯一、自增
  "title": "第一章 觉醒",
  "content": "正文……",
  "status": "editing",                 // draft=草稿 / editing=修改中 / finalized=已定稿
  "word_count": 50,                    // 去空白字符数，由后端按 content 重算
  "blueprint_snapshot": {},
  "linked_character_ids": ["char_000001"],   // 业务ID，见坑7
  "linked_location_ids": ["loc_000001"],
  "linked_item_ids": ["itm_000001"],
  "linked_rule_ids": ["rul_000001"],
  "summary": "本章摘要",
  "unresolved_threads": ["伏笔1", "伏笔2"],   // 前端每行一条
  "version": 1,
  "is_deleted": false,
  "created_at": "...", "updated_at": "...", "deleted_at": null
}
```

### 2. 状态机与定稿锁

- `draft（草稿）→ editing（修改中）→ finalized（已定稿）`，定稿走 `POST /{cid}/finalize`，重开走 `POST /{cid}/reopen`（回到 editing）。
- **已定稿后禁止改正文**：对 finalized 章节 PUT 带 `content` 直接 409；要改正文必须先 reopen。标题/关联等仍可调整。
- `number` 在同一 novel 内唯一，新建不传 number 时按 id_sequences 自增。

### 3. API（前缀 /api/chapters）

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/status-meta` | 状态枚举与中文标签 |
| GET | `/novel/{novel_id}?volume_id=&include_deleted=` | 列表（返回 `{data,total}`，列表项不含 content） |
| POST | `/novel/{novel_id}/create` | 新建（自动 chapter_id、number） |
| GET | `/{cid}?include_deleted=` | 详情（含正文） |
| PUT | `/{cid}?expected_version=N` | 增量更新（乐观锁，版本不符 409；自动重算 word_count） |
| POST | `/{cid}/finalize` / `/{cid}/reopen` | 定稿 / 重开 |
| DELETE | `/{cid}` | 软删 |
| POST | `/{cid}/restore` | 恢复 |
| GET | `/novel/{novel_id}/navigation/{number}` | 返回 `{prev,next}` 上下章 |

### 4. 前端章节编辑器（StoryContinuityWorkspace.tsx 的 ChapterEditorWorkspace）

- 三栏：左章节列表（含章节回收站）、中正文（查找/全部替换/字数/状态/上下章）、右关联实体 tag + 摘要 + 伏笔。
- 700ms 防抖自动保存，**用 pendingRef 累积连续改动**（快速改标题/正文/点多个 tag 不会互相覆盖），保存失败把变更放回队列下次重试。
- 乐观锁 409 时顶部出冲突横幅，可"重新加载服务器版本"。
- 关联 tag 点选即存，值为业务ID；连续点同一分组基于 pendingRef 计算，不丢选择。

---

## 八、旧版数据迁移（scripts/migrate_legacy.py）

把停用的 main 分支文件式数据（`Novel_Src/setting_cards.json`、`Novel_Src/chapters/*.txt`）导入新版 Mongo，**复用后端 Repository**，业务ID自动分配、字段结构与线上一致，**幂等可重复跑**（同名卡 / 同章节号自动跳过）。

```bash
cd /home/ubuntu/AI_NovelGenerator_17777
# 先空跑看会导入什么
.venv/bin/python scripts/migrate_legacy.py \
  --novel-id <小说ObjectId> \
  --src-dir /home/ubuntu/AI_NovelGenerator_main/Novel_Src --dry-run
# 确认无误后去掉 --dry-run 正式导入
.venv/bin/python scripts/migrate_legacy.py \
  --novel-id <小说ObjectId> \
  --src-dir /home/ubuntu/AI_NovelGenerator_main/Novel_Src
```

- 旧卡字段 → 新字段映射：location 的 `connections→related_characters`；item 的 `limitations→limitations`、`owner→owner`；rule 的 `scope/definition/constraints/exceptions/consequences` 同名迁移；未覆盖到的旧字段自动并入 `notes`，不丢内容。
- 章节文件名宽松解析：`第1章 标题.txt`、`第2章_标题.txt`、`chapter 3 - x.txt` 都能提取 number 与标题，文件全文作为正文。
- 当前 main 分支旧数据实际为空（`setting_cards.json` 为 `{"version":1,"cards":[]}`、chapters 空），脚本已用独立临时 novel 做过端到端导入 + 幂等 + 清理验证。

---

## 九、Windows 本机操作的坑（远程维护时）

- PowerShell **不支持 `&&` 串联命令**，两条命令分开发或用 `;`；且 PowerShell 把 `curl` 别名成 `Invoke-WebRequest`，**远程命令里不要直接写 curl 管道**，要测接口就写成 `.py` 上传跑。
- 通过 ssh_client.py 远程执行时，**不要在命令行里塞复杂内联 Python / 中文 JSON / `$()` / 引号嵌套**，PowerShell 会把引号、`$`、中文转义搞坏。正确做法：写 `.py` → `upload` 到 `/tmp` → `.venv/bin/python /tmp/xxx.py`。
- 前端构建约 20~40 秒，远程命令用 `--cmd-timeout 200` 并放后台，再取结果。
- 浏览器自动化（computer_use_tool / bu）：
  - 页面 reload 后 ref 全部失效且默认跳回"小说信息"页，要重新点进对应工作区。
  - 工作区顶部横向子标签（地点卡/物品卡/…）在窄屏下 `bu.click` 经常点空，**用 JS 按文本找按钮 `.click()` 最稳**：
    ```js
    Array.from(document.querySelectorAll('button')).find(b=>b.textContent.trim()==='物品卡')?.click();
    ```
  - 列表/选项是异步加载，点击后 sleep 2.5~3 秒再读，过快会读不到。
  - **一切以浏览器实际表现为唯一验收标准**，命令行构建通过 ≠ 功能可用。

---

## 十、章节与设定卡 AI 闭环（本次新增）

### 1. 两个新集合

| 集合 | 作用 | 关键约束 |
| --- | --- | --- |
| `generation_records` | 每次 AI 生成的输入快照、候选结果与采纳状态 | `generation_id` 业务ID（`gen_`，按小说隔离自增）；`request_id` 在同一 `novel_id + kind` 下唯一，重复提交直接复用候选 |
| `story_events` | 地点/物品/规则/角色的事实变更事件 | `event_id` 业务ID（`evt_`）；只有 `status=accepted` 的事件会更新正式卡片 |

- 两个集合都走统一软删除（`is_deleted` / `deleted_at`）。
- `generation_records.request_id` 为空时**不写该字段**：MongoDB 的 partial 索引不支持 `$ne`，幂等索引用 `{request_id: {$type: "string"}}` 过滤，空串会让多条记录互相冲突。
- `story_events.entity_id` 存业务ID（`itm_/loc_/rul_/char_`），不存 ObjectId。

### 2. 索引

`main.py` 启动时会调用 `init_all_indexes()`，新增了 `chapters`、`setting_cards`、`generation_records`、`story_events` 四组索引。
`chapters` 与 `setting_cards` 是**已有数据**上补索引，构建失败只会打 WARNING 不会阻断启动（历史脏数据可能违反新约束），日志里能直接看到原因。

### 3. 提示词

- 新增 12 个提示词分组（`generate_setting_card`、`complete_setting_card`、`extract_setting_cards`、`check_card_conflicts`、`chapter_plan`、`chapter_draft`、`chapter_continue`、`chapter_rewrite`、`chapter_expand`、`chapter_compress`、`consistency_review`、`propose_state_changes`），全部写在 `backend/llm/prompts/prompt_default.yaml`。
- 这些分组**不进入 REQUIRED_PROMPT_SECTIONS**：服务器上若已有自定义 `prompt.yaml`，缺少新分组时按分组回退默认文件，不会让整个自定义提示词文件校验失败。
- 启动日志里若出现「章节/设定卡 AI 提示词检查」，说明分组缺失或占位符不匹配，按提示补齐即可。

### 4. 事务与降级

`FinalizeChapterService` 用 `run_mongo_write_unit` 包裹章节定稿 + 卡片状态写回：

- 副本集/分片集：走真实事务；
- 单机 MongoDB（本机 Docker 单节点就是这种）：自动降级为顺序写入，日志会打印一次中文降级提示，**这是预期行为，不是错误**。

### 5. 定稿行为（重要）

- 定稿接口是两阶段：`stage=review` 只审校并生成待确认事件，`stage=commit` 才落库。
- 阻断级（blocking）问题未处理时定稿会被拒绝，必须显式确认忽略并传 `force=true`。
- **角色类事件只做确认记录**（返回在 `pending_character_updates`），不会自动改写角色档案——角色档案涉及关系与势力绑定联动，需要在角色页人工确认。

### 6. 验证脚本

```bash
cd /home/ubuntu/AI_NovelGenerator_17777
.venv/bin/python scripts/verify_chapter_ai_closure.py
```

脚本会自建一本临时小说验证「关联校验 → 上下文组装 → 定稿落库 → 重复定稿拒绝 → 序列分配」，
结束时自动清理，不会调用大模型。**改动章节/卡片相关后端代码后建议先跑它。**

### 7. 前端

- 章节编辑页新增「AI 写作助手」面板：生成方案 → 生成本章 → 采用正文 / 续写 / 选段改写扩写压缩 / 一致性审校 / 定稿确认。
- 三个卡片的编辑区下方新增「AI 设定助手」面板：生成 / 补全（含字段锁定）/ 从小说内容提取 / 冲突检查。
- 保存改为独立保存队列：切换章节、切换卡片、定稿前都会先 `flush()`，从根上修掉"快速切章把改动写到下一章"的问题；保存失败会把改动放回队列并在页面顶部提示。
- 前端改动必须重新 `npm run build` 再重启前端服务。

